/*angular.module('starter', ['ionic', 'ngCordova'])

.controller('setupController', ['$scope', listAlarms]);

function listAlarms($scope, $ionicPlatform) {

    $scope.defaultMinute = 0,
        $scope.defaultHour = 0,
        $scope.defaultName = "CoolAlarm",
        $scope.defaultTod = "PM";
    $scope.tod = ["AM", "PM"];
}
*/
